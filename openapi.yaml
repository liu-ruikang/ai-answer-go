# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: LLM API
    description: LLM服务定义
    version: 0.0.1
paths:
    /api/llm/deepseek-r1/chat:
        post:
            tags:
                - LLM
            description: 调用Deepseek R1模型
            operationId: LLM_ChatDeepseekR1
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ChatDeepseekR1Request'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ChatDeepseekR1Response'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/llm/deepseek-r1/stream-chat:
        post:
            tags:
                - LLM
            description: 流式调用Deepseek R1模型
            operationId: LLM_StreamChatDeepseekR1
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ChatDeepseekR1Request'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ChatDeepseekR1Response'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        ChatDeepseekR1Request:
            type: object
            properties:
                sessionId:
                    type: string
                messages:
                    type: array
                    items:
                        $ref: '#/components/schemas/Message'
                temperature:
                    type: number
                    format: float
                topP:
                    type: number
                    format: float
                maxTokens:
                    type: integer
                    format: int32
                stream:
                    type: boolean
            description: Deepseek R1请求
        ChatDeepseekR1Response:
            type: object
            properties:
                sessionId:
                    type: string
                content:
                    type: string
                tokenUsage:
                    $ref: '#/components/schemas/TokenUsage'
                model:
                    type: string
                errorMessage:
                    type: string
            description: Deepseek R1响应
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        Message:
            type: object
            properties:
                role:
                    type: string
                content:
                    type: string
            description: 消息结构定义
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        TokenUsage:
            type: object
            properties:
                promptTokens:
                    type: integer
                    format: int32
                completionTokens:
                    type: integer
                    format: int32
                totalTokens:
                    type: integer
                    format: int32
            description: Token使用统计
tags:
    - name: LLM
